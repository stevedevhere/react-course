import { css } from 'emotion';

const container = () => css`
  padding-top: 20px;
  p {
    text-align: center;
    font-size: 18px;
    color: white;  
    a {
      color: #165fbf;
      text-decoration: underline;
    }
  }
`;

export default ({
  container,
});
