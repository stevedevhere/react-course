import React from 'react';

const Header = (props) => (
    <div>
        <p className="description">
            <b>This is {`<Header/>`}</b>
        </p>
        <p>Тут должно быть более детальное описание сегодняшнего урока, но пока что его нет.</p>
    </div>
);

export default Header;
