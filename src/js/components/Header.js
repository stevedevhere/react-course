import React from 'react';

const Header = (props) => (
    <header className="header">
        <h1>React Lesson: 3 [ router, redux ]</h1>
        <h3>Lesson 3 preview!</h3>
    </header>
);

export default Header;
