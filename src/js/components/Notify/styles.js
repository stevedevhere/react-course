import { css } from 'emotion';

const notify = () => css`
  position: fixed;
  left: 40px;
  bottom: 40px;
  color: #000;
  padding: 8px 20px;
  background-color: #fff;
`;

export default ({
  notify,
});
